spring:
  application:
    name: libraryapi
  datasource:
    url: jdbc:postgresql://localhost:5432/library
    username: lazzy
    password: lazzy123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
